import { TodoList } from "@/components/TodoList";
import { Layout } from "@/components/Layout";

const Index = () => {
  return (
    <Layout>
      <TodoList />
    </Layout>
  );
};

export default Index;
